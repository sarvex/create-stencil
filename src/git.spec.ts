describe('git', () => {
  describe('inExistingGitTree', () => {
    it('todo', () => {
      fail('todo!');
    });
  });

  describe('initGit', () => {
    it('todo', () => {
      fail('todo!');
    });
  });

  describe('commitGit', () => {
    it('todo', () => {
      fail('todo!');
    });
  });
});
